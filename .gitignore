.DS_Store
.vercel
node_modules

# 출석 데이터는 공개 레포에 커밋하지 않는다.
# GitHub Action이 매번 새로 만들어 Vercel로 바로 배포한다(레포에는 남지 않음).
public/data.json
.env*
.env.local

# 교역자 명단·초기 비밀번호는 공개 레포에 두지 않는다.
scripts/roster.csv
